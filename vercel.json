{
  "version": 2,
  "outputDirectory": "dist",
  "framework": "vite",
  "buildCommand": "npm install --force && npx vite build",
  "installCommand": "npm install --force",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
